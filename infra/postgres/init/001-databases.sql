-- BINDERY BOX local stack bootstrap databases.
-- Values here are local-development defaults only; production should create
-- users/databases through the target environment's secret manager.
CREATE DATABASE bindery;
CREATE DATABASE mattermost;
